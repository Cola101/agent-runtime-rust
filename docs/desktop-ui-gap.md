# 桌面客户端能力清单（对标 Codex / OpenClaw）

- 日期：2026-08-17
- 范围：`desktop/`。**不含** Runtime 内核；内核缺口见 `docs/roadmap.md`

状态取值：`未做` / `待接入`（界面在、数据未接） / `已接入`

## 一、Codex / Claude Code 有而我们没有的

这些是终端形态的编码客户端已经证明必要的交互，不是猜测。

| # | 能力 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 命令面板（`/` 与 ⌘K） | 待接入 | 扩展模型的核心：新功能一律先落这里 |
| 2 | 文件引用（`@` 提及、拖放、粘贴图片） | 未做 | 编码客户端的主要输入方式之一 |
| 3 | 计划模式 / 权限模式切换 | 未做 | Codex 与 CC 都把它放在状态行常驻 |
| 4 | 运行中的待办清单 | 未做 | 模型自己维护的任务列表，实时渲染 |
| 5 | 推理过程显示（默认折叠） | 未做 | Runtime 已有 typed reasoning item，UI 无入口 |
| 6 | Diff 逐块接受 / 拒绝 | 未做 | 现在只渲染 diff，不能审阅 |
| 7 | 运行中排队输入 | 未做 | 边跑边打字，跑完依次生效 |
| 8 | 中途改向（steer） | **阻塞** | `runtime-host` 无 steer 动作，见下 |
| 9 | 转录搜索（⌘F） | 未做 | |
| 10 | 模型快速切换 | 未做 | 设置里有分区，缺快捷入口 |
| 11 | 命令历史（↑） | 未做 | |
| 12 | 多行输入与编辑键位 | 未做 | 当前输入框是静态占位 |
| 13 | 原生通知 | 未做 | 后台时"有事等你"必须能穿透 |
| 14 | 会话恢复 / 继续 | 待接入 | Runtime 侧已完备，UI 未接 |
| 15 | 键位速查 | 未做 | |
| 16 | 导出 / 分享转录 | 未做 | |

## 二、OpenClaw 有而我们没有的

| # | 能力 | 状态 | 备注 |
| --- | --- | --- | --- |
| 17 | 节点 / 设备列表 | 未做 | 多 Runtime 时的切换与健康 |
| 18 | 连接生命周期可视 | 待接入 | 断线、重连、从哪个序号续上 |
| 19 | 离线任务队列 | 未做 | Edge 方向暂停，UI 先不做 |
| 20 | 模型容灾可视 | 未做 | Runtime 已有 route journal，UI 无入口 |
| 21 | Workspace 冲突处理 | 未做 | 同上，且不得静默覆盖 |

## 三、这个 Runtime 独有、UI 却完全没体现的

**最容易被忽略的一类：内核已经做完并有证据，界面上却看不见。**

| # | 能力 | 状态 | 备注 |
| --- | --- | --- | --- |
| 22 | 子代理树（角色 / 深度 / 并发 / 预算子集） | 未做 | 内核做得最深的一块，UI 一个像素都没有 |
| 23 | Session fork / rollback | 待接入 | 详情页工具栏有按钮，无界面 |
| 24 | 持久进程会话 / PTY | 未做 | `process.*` 工具族，需要终端面 |
| 25 | MCP resources / prompts 浏览 | 未做 | ADR-0116/0117 已实现读取契约 |
| 26 | `indeterminate` 人工裁决 | 待接入 | 已在 Approvals 队列里设计 |
| 27 | 事件游标 / 原始事件检查器 | 待接入 | 抽屉里有位置 |
| 28 | 每工具审批策略 | 待接入 | 设置里的 Tools & access |
| 29 | 预算与配额 | 待接入 | 状态行 + 抽屉 |
| 30 | 容器边界能力可视（ADR-0122） | 待接入 | 哪些保证这台机器给不了 |

## 四、被 Runtime 侧阻塞的

| 能力 | 阻塞原因 |
| --- | --- |
| 中途改向（#8） | `runtime-host` 的 `RuntimeControlAction` 只有 Resume / DecideApproval / Cancel / ResolveMcpInput。**没有 steer**，本地适配器也没有。这不是 UI 问题 |
| 真实连接（全部） | 需要 Node 侧 gRPC 客户端；Rust 客户端已证明契约可用，但 Electron 主进程是 Node |

## 五、推进顺序

1. **面骨架**：Runs / Approvals / Settings / Run detail 注册进外壳，可导航 ← *进行中*
2. **共享构件**：命令面板、抽屉、身份气泡、每面工具栏
3. **Node gRPC 客户端**：`@grpc/grpc-js` + 同一份 proto。做完后契约被两种语言各自生成，"协议中立"从声称变为事实
4. **只读接入**：Runs 列表与转录接真数据
5. **决策接入**：审批与裁决走 `Control`
6. **编码客户端交互**：文件引用、diff 审阅、待办清单、排队输入
7. **steer**：需先在 Runtime 侧补 `RuntimeControlAction`

## 不做

- 仪表盘、小组件网格、第二侧栏
- 离线队列与 Workspace 三方合并（Edge 方向暂停）
- 任何在 Runtime 侧无对应契约的功能——UI 不发明后端不存在的能力
